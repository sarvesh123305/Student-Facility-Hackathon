import React from 'react'

const Elective = (props) => {
  return (
    <div>
        {/* <h1>Departmental Elective</h1>
        <br></br>

        {data && data.Eubjects && data.Electives.map((item, index) => (
            console.log(data)
        ))}

        <form class="max-w-sm mx-auto">
            <label for="countries" class="block mb-2 text-sm font-medium text-gray-900 ">Preference 1</label>
            <select id="countries" class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 ">
                <option selected>Choose Elective</option>
                <option value="WST">WST</option>
                <option value="SP">SP</option>
                <option value="ADS">ADS</option>
            </select>
        </form> */}
   { console.log(props.subjects)}
    </div>
  )
}

export default Elective